<!DOCTYPE html>
<html>

<body>
    <h2>Test Checkbox Event</h2>

    <form method="post">
      <input type="checkbox" name="course[]" value="PHP">PHP
      <input type="checkbox" name="course[]" value="Python">Python
      <input type="checkbox" name="course[]" value="Excel">Excel
      <input type="checkbox" name="course[]" value="Javascript">Javascript
      <input type="checkbox" name="course[]" value="Java">Java<br> <br>
      <input type="submit" name="submit" value="Select">
    </form>

    <?php
    if (isset($_POST['submit'])) {
      if (isset($_POST['course'])) {
        $Courses = $_POST['course'];
        echo "Checkbox selected:<br><br>";

        foreach ($Courses as $selectedCourse) {
          echo "You selected: " . htmlspecialchars($selectedCourse) . "<br>";
        }
      } else {
        echo "No checkboxes selected.";
      }
    }
    ?>
</body>

</html>
